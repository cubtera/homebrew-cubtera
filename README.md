# Homebrew Cubtera Formula

Install `Cubtera` with Brew:
```bash
brew tap cubtera/cubtera
brew install cubtera
```

Repo contains Cubtera version's Formulas and GH action for automatic update's commits. 
